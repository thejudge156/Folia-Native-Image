--- a/paper-server/build.gradle.kts
+++ b/paper-server/build.gradle.kts
@@ -9,6 +_,7 @@
     idea
     id("io.papermc.paperweight.core")
     id("io.papermc.fill.gradle") version "1.0.12"
+    id("org.graalvm.buildtools.native") version "1.1.9"
 }
 
 val paperMavenPublicUrl = "https://repo.papermc.io/repository/maven-public/"
@@ -20,13 +_,30 @@
 
 paperweight {
     minecraftVersion = providers.gradleProperty("mcVersion")
+    filterPatches = false
     gitFilePatches = false
 
+    val fork = forks.register("folia") {
+        upstream.patchDir("paperServer") {
+            upstreamPath = "paper-server"
+            excludes = setOf("src/minecraft", "patches", "build.gradle.kts")
+            patchesDir = rootDirectory.dir("folia-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+    }
+
+    activeFork = fork
+
+
     updatingMinecraft {
         // oldPaperCommit = "d4fe85375af18bfa88f44d7c1e6a61904ae550cc"
     }
 }
 
+repositories {
+    mavenLocal()
+}
+
 tasks.generateDevelopmentBundle {
     libraryRepositories.addAll(
         "https://repo.maven.apache.org/maven2/",
@@ -86,7 +_,19 @@
     }
 }
 
-val log4jPlugins = sourceSets.create("log4jPlugins")
+sourceSets {
+    main {
+        java { srcDir("../paper-server/src/main/java") }
+        resources { srcDir("../paper-server/src/main/resources") }
+    }
+    test {
+        java { srcDir("../paper-server/src/test/java") }
+        resources { srcDir("../paper-server/src/test/resources") }
+    }
+}
+val log4jPlugins = sourceSets.create("log4jPlugins") {
+    java { srcDir("../paper-server/src/log4jPlugins/java") }
+}
 configurations.named(log4jPlugins.compileClasspathConfigurationName) {
     extendsFrom(configurations.compileClasspath.get())
 }
@@ -109,8 +_,8 @@
 }
 
 dependencies {
-    implementation(project(":paper-api"))
-    implementation("ca.spottedleaf:leafpile:1.1.0")
+    implementation(project(":folia-api"))
+    implementation("ca.spottedleaf:leafpile:1.0.0")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
     implementation("net.minecrell:terminalconsoleappender:1.3.0")
@@ -124,7 +_,7 @@
      */
     implementation("org.apache.logging.log4j:log4j-core:2.26.0")
     log4jPlugins.annotationProcessorConfigurationName("org.apache.logging.log4j:log4j-core:2.26.0") // Needed to generate meta for our Log4j plugins
-    runtimeOnly(log4jPlugins.output)
+    //runtimeOnly(log4jPlugins.output)
     alsoShade(log4jPlugins.output)
 
     implementation("com.velocitypowered:velocity-native:3.4.0-SNAPSHOT") {
@@ -156,7 +_,7 @@
 
     // Spark
     implementation("me.lucko:spark-api:0.1-20240720.200737-2")
-    implementation("me.lucko:spark-paper:1.10.177")
+    implementation("me.lucko:spark-paper:1.10.172")
 }
 
 tasks.jar {
@@ -171,14 +_,14 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Paper",
+            "Implementation-Title" to "Folia",
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Paper",
+            "Specification-Title" to "Folia",
             "Specification-Version" to project.version,
             "Specification-Vendor" to "Paper Team",
-            "Brand-Id" to "papermc:paper",
-            "Brand-Name" to "Paper",
+            "Brand-Id" to "papermc:folia",
+            "Brand-Name" to "Folia",
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
@@ -246,7 +_,7 @@
     jvmArgumentProviders.add(provider)
 }
 
-val generatedDir: java.nio.file.Path = layout.projectDirectory.dir("src/generated/java").asFile.toPath()
+val generatedDir: java.nio.file.Path = layout.projectDirectory.dir("../paper-server/src/generated/java").asFile.toPath()
 idea {
     module {
         generatedSourceDirs.add(generatedDir.toFile())
@@ -327,7 +_,7 @@
 }
 
 fill {
-    project("paper")
+    project("folia")
     versionFamily(paperweight.minecraftVersion.map { it.split(".", "-").takeWhile { part -> part.toIntOrNull() != null }.take(2).joinToString(".") })
     version(paperweight.minecraftVersion)
 
@@ -340,5 +_,74 @@
                 nameResolver.set { project, _, version, build -> "$project-$version-$build.jar" }
             }
         }
+    }
+}
+
+graalvmNative {
+    agent {
+        defaultMode.set("standard") // Default agent mode if one isn't specified using `-Pagent=mode_name`
+        enabled.set(false) // Enables the agent
+
+        metadataCopy {
+            metadataCopy {
+                inputTaskNames.add("runServer")
+                outputDirectories.add("src/main/resources/META-INF/native-image/me.judge")
+                mergeWithExisting.set(true)
+            }
+        }
+    }
+    layers {
+        create("folia-api") {
+            contents {
+                modules("java.base", "jdk.jfr", "java.scripting", "java.sql", "java.management")
+                fromConfiguration(configurations.runtimeClasspath)
+                packages("net.minecraft", "com.mojang.logging", "com.mojang.authlib.yggdrasil", "io.papermc.paper", "org.spigotmc", "org.bukkit")
+            }
+            buildArgs.add("--initialize-at-run-time=io.netty")
+        }
+    }
+    binaries {
+        named("main") {
+            classpath(tasks.jar)
+            imageName.set("folia")
+            mainClass.set("org.bukkit.craftbukkit.Main")
+            sharedLibrary.set(false)
+        }
+        create("gatherPGO") {
+            classpath(tasks.jar)
+            classpath(configurations.runtimeClasspath)
+            imageName.set("folia")
+            mainClass.set("org.bukkit.craftbukkit.Main")
+            buildArgs.add("--pgo-instrument")
+            sharedLibrary.set(false)
+        }
+        create("PGO") {
+            classpath(tasks.jar)
+            classpath(configurations.runtimeClasspath)
+            imageName.set("folia")
+            mainClass.set("org.bukkit.craftbukkit.Main")
+            buildArgs.add("--pgo=$projectDir/pgo-profiles/default.iprof")
+            sharedLibrary.set(false)
+        }
+        named("test") {
+            classpath(tasks.jar)
+            classpath(configurations.runtimeClasspath)
+            buildArgs.add("-O0")
+        }
+    }
+    binaries.all {
+        usesLayer("folia-api")
+        buildArgs.addAll(
+            "--verbose", "--enable-http", "--enable-https",
+            "-H:+AddAllCharsets", "-H:+IncludeAllLocales", "-H:IncludeResources=data/.*", "-march=compatibility",
+            "-H:-ReduceImplicitExceptionStackTraceInformation", "--add-opens=java.base/java.lang.invoke=ALL-UNNAMED"
+        )
+    }
+    layers.all {
+        buildArgs.addAll(
+            "--verbose", "--enable-http", "--enable-https",
+            "-H:+AddAllCharsets", "-H:+IncludeAllLocales", "-H:IncludeResources=data/.*", "-march=compatibility",
+            "-H:-ReduceImplicitExceptionStackTraceInformation", "--add-opens=java.base/java.lang.invoke=ALL-UNNAMED"
+        )
     }
 }
